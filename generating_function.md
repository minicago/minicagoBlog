```metadata
title: 生成函数
date: 2026-06-23 14:00
category: 数学
difficulty: medium+
```

## 数列与函数

数列是我们信息学竞赛常常需要处理的一种数学上的离散结构：

通常我们用$\{a_n\}$表示数列，其实表示的是一种**离散的映射关系**，即可以用一个离散的函数表示数列。这种映射关系可以显式地写为：

$$a_n = f(n), \quad n \in \mathbb{N}^*$$

与连续函数不同，数列的自变量只能取离散的整数值，因此它的图像是一系列孤立的点，这正是“离散结构”的含义。

### 简单数列和其离散函数

**等差数列（一次函数型）**

若首项为 $a_1$，公差为 $d$，则通项公式为：

$$a_n = a_1 + (n-1)d$$

对应的离散函数为：

$$f(n) = a_1 + (n-1)d, \quad n \ge 1$$

当 $d \neq 0$ 时，$f(n)$ 是 $n$ 的一次函数，图像是一条直线上均匀分布的离散点。

**示例**：$a_1 = 2, d = 3$ 时，
$$f(1)=2,\; f(2)=5,\; f(3)=8,\; \dots$$

**等比数列（指数函数型）**

若首项为 $a_1$，公比为 $q$（$q \neq 0$），则通项公式为：

$$a_n = a_1 \cdot q^{n-1}$$

对应的离散函数为：

$$f(n) = a_1 \cdot q^{n-1}, \quad n \ge 1$$

这是 $n$ 的指数型函数，图像是指数曲线上的离散采样点。

**示例**：$a_1 = 3, q = 2$ 时，
$$f(1)=3,\; f(2)=6,\; f(3)=12,\; \dots$$

### 复杂数列处理

当我们尝试去表示一些复杂的数列时，就很难直接给出其对应的离散函数的初等表示，如斐波拉契数列：

$$F_0 = 0,\; F_1 = 1,\; F_n = F_{n-1} + F_{n-2}$$

此时我们可以尝试换一种方式表示该函数：**母函数**

**母函数定义**

对于数列 $\{a_n\}_{n=0}^{\infty}$，其普通母函数（Ordinary Generating Function）定义为形式幂级数：

$$G(x) = \sum_{n=0}^{\infty} a_n x^n$$

数列的每一项对应幂级数中 $x^n$ 的系数。这样，离散问题转化为幂级数的代数运算。

>这个变换类似于：点值表示法转为插值表示法，恰好是之前卷积操作中变换的逆操作

### 提前实践

数学往往不是先有定义的，计算和应用往往是“提前偷跑”。

我们先不必纠结为什么要这样定义，我们先来尝试找几个例子：

**等差数列**

对于
\[
a_n = a_1 + (n-1)d \quad (n \ge 1)
\]

令 \( m = n-1 \)，则：

\[
G(x) = \sum_{n=1}^{\infty} [a_1 + (n-1)d] x^n = x \sum_{m=0}^{\infty} (a_1 + m d) x^m
\]

**等比数列**

对于 
\[
a_n = a_1 \cdot q^{n-1} \quad (n \ge 1)
\]

则母函数为：

\[
G(x) = \sum_{n=1}^{\infty} a_1 q^{n-1} x^n = a_1 x \sum_{m=0}^{\infty} (qx)^m = \frac{a_1 x}{1 - qx}
\]

>可以看出等比数列就有很美的形式

**对于有递推公式的数列**

假设 $a_{n+2} = \alpha \, a_{n+1} + \beta \, a_{n}$

\[
G(x) = \sum_{n=1}^{\infty} a_n x^n 
\]

\[
\alpha \, x\, G(x) = \sum_{n=2}^{\infty} \alpha a_{n-1} x^n
\]

两式相减：

\[
G(x) - \alpha \, x\, G(x) = a_1 + \sum_{n=2}^{\infty} a_{n} - \alpha a_{n-1} x^n \\ = (1 - \alpha) a_1 + a_2 + \sum_{n=3}^{\infty} \beta a_{n-2} x^n = (1 - \alpha) a_1 + a_2  + x^2 \cdot \beta \cdot G(x)
\]

解得 
\[
G(x) = \frac{(1 - \alpha) a_1 + a_2}{1 - \alpha - x^2 \cdot \beta}
\]

数学基础好的同学不难发现分母可以拆分成两个分母含$x$一次方相乘的形式，也就可以拆分成两个等比数列的和。

**母函数进行基本运算的结果**

尝试对母函数进行各种运算后得到：

数列 \(\{a_n\}\) 的普通母函数为 \(G(x)=\sum_{n\ge0} a_n x^n\)。

| 操作类型 | 母函数运算 | 新数列通项 \(b_n\) | 离散含义 / 组合解释 |
| :--- | :--- | :--- | :--- |
| **右移位（添加前导零）** | \(x^k G(x)\) | \(b_n = \begin{cases} a_{n-k}, & n\ge k \\ 0, & n<k \end{cases}\) | 将原数列整体向后平移 \(k\) 位，即 \(b_n = a_{n-k}\)。 |
| **乘以 \(n\)（加权）** | \(xG'(x)\) | \(b_n = n \cdot a_n\) | 给第 \(n\) 项乘以下标 \(n\)，常用于求期望或处理阶乘因子。 |
| **数列卷积（乘积）** | \(A(x)B(x)\) | \(c_n = \sum_{k=0}^n a_k \, b_{n-k}\) | 普通卷积，对应无序组合选取（选 \(k\) 个给 A，剩下给 B）（计数背包中合并两块结果的操作）。 |
| **线性组合** | \(\alpha A(x)+\beta B(x)\) | \(c_n = \alpha a_n + \beta b_n\) | 数列对应项的线性叠加，保持离散映射线性。 |
| **变量代换（缩放）** | \(G(cx)\) | \(b_n = c^n \cdot a_n\) | 对项乘以公比 \(c^n\)，即 \(a_n\) 按指数缩放。 |
| **前缀和（求和）** | \(\dfrac{G(x)}{1-x}\) | \(b_n = \sum_{k=0}^n a_k\) | 除以 \((1-x)\) 等价于求离散前缀和，将 \(f(n)\) 变为累积和。 |

### 扩展：指数型母函数

同时还有指数型母函数定义为形式幂级数：

\[
E(x) = \sum_{n=0}^{\infty} a_n \frac{x^n}{n!}
\]

与普通母函数 \(G(x) = \sum a_n x^n\) 相比，这里每一项多除了一个 \(n!\)。这种“除阶乘”的编码方式，使得它在处理**有序结构**时具有极其优雅的组合意义。

| 操作类型 | 母函数运算 | 新数列通项 \(c_n\) | 离散含义 / 组合解释 |
| :--- | :--- | :--- | :--- |
| **左移位（去掉前导项）** | \(E'(x)\) | \(c_n = a_{n+1}\) | 干净左移，无额外因子，非常适合处理递推中的下标前进。 |
| **积分（插入前导零）** | \(\int_0^x E(t)\,dt\) | \(c_0 = 0,\quad c_n = a_{n-1}\ (n\ge1)\) | 干净右移，无分母 \(n\)，便于逆操作。 |
| **数列卷积（乘积）** | \(A(x)B(x)\) | \(c_n = \sum_{k=0}^n \dbinom{n}{k} a_k \, b_{n-k}\) | 二项式卷积，对应有序划分：先选 \(k\) 个给 A，剩余给 B，并考虑排列。 （考略排列的计数背包中合并两块结果的操作）|
| **线性组合** | \(\alpha A(x)+\beta B(x)\) | \(c_n = \alpha a_n + \beta b_n\) | 数列对应项的线性叠加，性质保持。 |
| **变量代换（缩放）** | \(E(cx)\) | \(c_n = c^n \cdot a_n\) | 对项乘以公比 \(c^n\)，与 OGF 一致。 |
| **前缀和（求和）** | （不常用） | 无标准形式 | EGF 中求和操作通常用积分或微分组合实现，不直接除以 \((1-x)\)。 |

指数函数（exp） \(e^x\) 在 \(x=0\) 处的泰勒展开（麦克劳林级数）为：

\[
\boxed{e^x = \sum_{n=0}^{\infty} \frac{x^n}{n!} = 1 + x + \frac{x^2}{2!} + \frac{x^3}{3!} + \cdots + \frac{x^n}{n!} + \cdots}
\]

## 斯特林数

### 第一类斯特林数 \(\displaystyle {n \brack k}\) （或 \(s(n, k)\)）

第一类斯特林数分为**无符号**（计数排列）和**有符号**（用于多项式系数），信息学竞赛中绝大多数场景使用**无符号第一类斯特林数**。

#### 定义（无符号）
表示将 \(n\) 个**不同的元素**划分为 \(k\) 个**非空无序循环排列**（即圆排列 / 轮换）的方案数。

**边界条件**：
\[
{n \brack 0} = [n=0], \quad {n \brack n} = 1, \quad {n \brack k} = 0 \ (k > n)
\]

#### 递推计算公式
\[
\boxed{{n \brack k} = (n-1) \cdot {n-1 \brack k} + {n-1 \brack k-1}}
\]
**解释**：考虑第 \(n\) 个元素，要么单独成为一个循环（贡献 \({n-1 \brack k-1}\)），要么插入到前 \(n-1\) 个元素形成的某个循环中。对于每个循环，插入位置共有 \((n-1)\) 种（因为每个循环中，一个元素后面可以插入新元素），故贡献 \((n-1) \cdot {n-1 \brack k}\)。

#### 生成函数（OGF）—— 固定 \(n\)（上升阶乘）
无符号第一类斯特林数最核心的性质是：
\[
\boxed{\sum_{k=0}^{n} {n \brack k} x^k = x(x+1)(x+2)\cdots(x+n-1) = x^{\overline{n}}}
\]
其中 \(x^{\overline{n}}\) 为**上升阶乘**（Rising Factorial）。

**有符号版本**（带符号第一类 \(s(n,k)\)）对应下降阶乘：
\[
\sum_{k=0}^{n} s(n,k) x^k = x(x-1)(x-2)\cdots(x-n+1) = x^{\underline{n}}
\]
（两者关系：\({n \brack k} = (-1)^{n-k} \cdot s(n,k)\)）

#### 指数母函数（EGF）—— 固定 \(k\)
\[
\boxed{\sum_{n \ge k} {n \brack k} \frac{x^n}{n!} = \frac{[-\ln(1-x)]^k}{k!}}
\]
**解读**：单个非空循环的 EGF 为 \(\sum_{n\ge1} (n-1)! \frac{x^n}{n!} = -\ln(1-x)\)。\(k\) 个无序循环的集合即为 \(\frac{[-\ln(1-x)]^k}{k!}\)。


### 第二类斯特林数 \(\displaystyle {n \brace k}\) （或 \(S(n, k)\)）

#### 定义
表示将 \(n\) 个**不同的元素**划分为 \(k\) 个**非空无序子集**（块）的方案数。

**边界条件**：
\[
{n \brace 0} = [n=0], \quad {n \brace n} = 1, \quad {n \brace k} = 0 \ (k > n)
\]

#### 递推计算公式（最常用）
\[
\boxed{{n \brace k} = k \cdot {n-1 \brace k} + {n-1 \brace k-1}}
\]
**解释**：考虑第 \(n\) 个元素，要么单独成一块（贡献 \({n-1 \brace k-1}\)），要么放入已有的 \(k\) 个块中之一（有 \(k\) 种选择，贡献 \(k \cdot {n-1 \brace k}\)）。

#### 通项公式（容斥原理 / 显式求和）
\[
\boxed{{n \brace k} = \frac{1}{k!} \sum_{i=0}^{k} (-1)^{k-i} \binom{k}{i} i^n}
\]


#### 指数母函数（EGF）—— 固定 \(k\)
\[
\boxed{\sum_{n \ge k} {n \brace k} \frac{x^n}{n!} = \frac{(e^x - 1)^k}{k!}}
\]
**解读**：由于单个非空块的 EGF 为 \(e^x - 1\)（因为空集不能作为块），\(k\) 个无序块即为 \(\frac{(e^x - 1)^k}{k!}\)。这正是我们之前推导的 SET 构造的直接应用。