```metadata
title: 背包数学模型
date: 2026-06-03 12:00
category: 动态规划
difficulty: medium
```

## 背包问题

> 题意概要：[「USACO07 DEC」Charm Bracelet](https://www.luogu.com.cn/problem/P2871) 有$n$个物品和一个容量为$W$的背包，每个物品有重量$w_i$和价值$v_i$两种属性，要求选若干物品放入背包使背包中物品的总价值最大且背包中物品的总重量不超过背包的容量。

我们设计状态$f_{i,j}$表示仅考虑前$i$个物品， 花费 $j$ 重量的最大价值。

有状态转移方程：

$$f_{i,j} = \max \{ f_{i - 1,j - w_i} + v_i , f_{i-1, j} \}$$

## 函数视角

考虑函数最大价值函数$g(x) = f_{n,x}$， 则函数$g(x)$表示使用$x$空间的最大收益。

如果考虑对于两**组**物品，我们的最大价值函数
分别为$g_1(x),g_2(x)$，现在考虑可以在这两组物品中任意选择的最大收益价值$g_3(x)$，有

$$g_3(x) = (g_1 \oplus g_2)(x) = \max_{0 \le i \le x} \{ g_1(i) + g_2(x-i) \} $$

> 这里的 $\oplus$ 符号表示“合并”两种组物品后求最大值的操作，被称为**max-plus卷积**，容易发现其具有交换律和结合律。
>考虑在第一组中使用$i$的空间，在第二组中使用$x-i$的空间，能获得的最大价值为$g_1(i) + g_2(x-i)$。

下面给出该方法的代码实现：

```c++
for (int i = 0; i <= W; i++){
    // 计算 g3(i)
    for (int j = 0; j <= W; j++){
        g3[i] = max(g1[j] + g2[i - j], g3[i]);
    }
}
```

>可见**max-plus卷积**的常规计算时间复杂度为 $O(W^2)$， 而合并次数为 n， 复杂度为 $O(nW^2)$
>而传统动态规划的计算方法时间复杂度为 $O(nW)$
>如果没有额外限制该思路对我们解决该问题似乎没有帮助，但其适用范围更广。
>但其实这个**max-plus卷积**可以构造一个矩阵
$$ M_x(i,j) =
g_x(i-j)
$$
>我们对于 $y < 0 $ 可以定义 $g_x(y) = -\infty$
>例如
$$ M_x =
\begin{pmatrix}
g_x(0) & g_x(1) & g_x(2) \\
-\infty & g_x(0) & g_x(1) \\
-\infty & -\infty & g_x(0)
\end{pmatrix}
$$
> 
> $$(M_1 \oplus M_2)(i,j) = g(j-i) = max(g_1(k-i) + g(j-k))$$
> 这样如果W很小，n达到一个非常大的值就可以使用max-plus矩阵快速幂优化
>> 矩阵乘法运算公式 $$
C_{ij} = \sum_{k=1}^{n} A_{ik} \cdot B_{kj}
$$
>> max-plus 矩阵乘法
$$
C_{ij} = \max_{k=1}^{n} \left( A_{ik} + B_{kj} \right)
$$

我们各种类型的背包问题都符合该计算方式，对于01背包问题，我们相当于将若干个下述函数合并：

$$
g(x) =
\begin{cases}
0, x < w_i \\\\
v_i,  x \ge w_i
\end{cases}
$$

而对于完全背包，该函数变为：

$$
g(x) = \lfloor \frac{x}{w_i} \rfloor \cdot v_i
$$

同时，当背包有依赖关系，我们可以考虑建树，对于父亲节点有：

$$
g_{fa}(x) =
\begin{cases}
0, x < w_{fa} \\\\
v_{fa} + \oplus_{y \in son_{fa}} g_{y}(x - w_{fa}),  x \ge w_{fa}
\end{cases}
$$

## 背包问题计数变种

> [小 A 点菜](https://www.luogu.com.cn/problem/P1164) 题目概要:有 $n$ 道菜，每道菜价格为 $a_i$，手上有 $M$ 元钱。求有多少种点菜方案，使得所选菜品的总价格 **恰好等于** $M$。

设 $dp[j]$ 表示花费恰好 $j$ 元的总方案数，初始 $dp[0] = 1$，其余为 $0$。

对于每道菜的价格 $a_i$（$1 \le i \le n$），采用 **0/1 背包** 的逆序更新：

$$
dp[j] = dp[j] + dp[j - a_i], \quad j = M \rightarrow a_i
$$

最终答案为 $dp[M]$。

考虑方案数函数 $f(x)$ 表示**花费恰好 $x$ 元的点菜方案数**，则原问题答案为 $f(M)$。

若有两组菜品，其方案数函数分别为 $f_1(x)$ 和 $f_2(x)$，则两组菜品合并后（可以任意从两组中选菜，每组内部菜不可重复选，且两组互不干扰）的方案数函数 $f_3(x)$ 为两者的**卷积**：

$$
f_3(x) = (f_1 * f_2)(x) = \sum_{i=0}^{x} f_1(i) \cdot f_2(x-i)
$$

这个$*$就是卷积操作，可以使用fft快速计算，也可以计算逆向操作（回滚背包）。

