```metadata
title: 入门须知：数学符号
date: 2026-05-17 00:00
category: 入门须知
difficulty: easy
```

## 写在前面

这篇文章将介绍算法竞赛中常用的数学符号和数学知识。我们将从最简单的符号开始，逐步深入，帮助你建立坚实的数学基础。

**学习建议**：不要急于一口气看完所有内容。建议先理解基础符号，然后在阅读其他文章时遇到不懂的符号再来查阅。

---

## 第一部分：最基础的数学符号（像学习ABC一样简单）

### 1. 下标表示 - 给每个东西起个名字

**初中理解**：就像班里每个同学都有座位号一样，数学中我们也给不同的东西编号。

**例子**：
- $a_1$ 表示"第一个a"
- $a_2$ 表示"第二个a"
- $dp_i$ 表示"第i个dp值"

**为什么需要下标？**
当我们有很多个相同类型的东西时（比如数组中的元素），就需要用下标来区分它们。

**常见用法**：
- $a_{i,j}$ 表示"第i行第j列的元素"（就像教室第i排第j列的同学）
- $f_{i,m}$ 表示"第i个状态中，第m个属性的值"

**记忆口诀**：下标就是"身份证号码"，告诉你是第几个。

---

### 2. 不等式符号 - 比较大小

**小学就学过的符号**：
- $<$ 小于（严格小于，不能等于）
- $>$ 大于（严格大于，不能等于）
- $\le$ 小于或等于（可能等于）
- $\ge$ 大于或等于（可能等于）

**在算法中的应用**：
- 循环条件：$i < n$ 表示"i还没到n"
- 约束条件：$L_j \le p_{ij} \le R_j$ 表示"p_{ij}必须在L_j和R_j之间"

**易错点**：
- $i < n$ 和 $i \le n-1$ 是等价的，但写代码时容易混淆
- 区间表示中，$[L, R)$ 表示从L到R但不包括R，$[L, R]$ 表示从L到R且包括R

---

### 3. 逻辑符号 - "所有的"和"并且"

**$\forall$ 符号**（读作"对于所有"或"任意"）：

**生活类比**：就像老师说"全班每个同学都要交作业"

**数学表示**：$\forall i = 1, 2, \ldots, n$ 表示"对于i从1到n的所有值"

**例子**：
$$\forall j = 1, 2, \ldots, B: \quad L_j \leq p_{ij} \leq R_j$$
意思是：对于j从1到B的每一个值，都要满足$L_j \leq p_{ij} \leq R_j$这个条件。

**$\And$ 符号**（逻辑"与"）：

**生活类比**：就像申请奖学金需要"成绩好 **并且** 品德好"

**数学表示**：$j < i \And S_i - S_j > dp_j$ 表示两个条件都要满足

---

### 4. 分数表示 - 除法的另一种写法

**小学学过的除法**：
- $n/2$ 和 $\frac{n}{2}$ 是一样的，都表示"n除以2"

**为什么要用分数线写法？**
当分子分母很复杂时，分数线写法更清晰：
- $n/\ln(n)$ 看起来有点乱
- $\frac{n}{\ln(n)}$ 看起来更清晰

**在算法中的例子**：
- $\frac{n}{2}$ 表示一半的数量（比如"最多n/2个新成员"）
- $\frac{a}{b}$ 表示比例或除法结果

---

## 第二部分：运算符号（初中水平的运算）

### 5. 求和符号 $\sum$ - 把一堆数加起来

**初中理解**：就是把很多数累加，像一个循环加法。

**基本形式**：
$$\sum_{i=1}^{n} a_i$$

**意思是**：把$a_1, a_2, a_3, \ldots, a_n$全部加起来

**等价代码**：
```cpp
int sum = 0;
for (int i = 1; i <= n; i++) {
    sum += a[i];
}
```

**扩展形式**：
- $\sum_{i=l}^{r} a_i$：从第l个加到第r个
- $\sum_{i=l}^{r} \sum_{j=i+1}^{r} h(i,j)$：双重求和（先固定i，对每个i再求j的和）

**生活类比**：就像计算一个月的总支出，把每天的花费加起来。

---

### 6. 求最值符号 $\min$ 和 $\max$ - 找最大最小

**小学就学过**：在一堆数中找最大值或最小值。

**数学表示**：
- $\min_{j < i} \{dp_j + cost(j)\}$：在所有$j < i$的情况中，找$dp_j + cost(j)$的最小值
- $\max_{x \in X} f(x)$：在集合X中找$f(x)$的最大值

**等价代码**：
```cpp
int min_val = INT_MAX;
for (int j = 0; j < i; j++) {
    min_val = min(min_val, dp[j] + cost(j));
}
```

**理解技巧**：
- $\min$后面的下标范围就是"在哪些选项中找"
- 大括号里的内容就是"比较什么值"

---

### 7. 模运算符号 - 取余数

**小学学过的余数**：
- $7 \div 3 = 2$ 余 $1$
- 用数学符号写：$7 \% 3 = 1$ 或 $7 \equiv 1 \pmod{3}$

**两种写法**：
- C++代码中：`a % b` 表示a除以b的余数
- 数学公式中：$a \equiv r \pmod{m}$ 表示"a除以m的余数是r"

**常见用途**：
1. 检查奇偶：$n \% 2 == 0$ 表示偶数
2. 防止数组越界：$i \% n$ 保证结果在$[0, n-1]$范围内
3. 循环处理：圆环上的位置常用模运算

**同余方程（以费马小定理为例）**：
$$a^{p-1} \equiv 1 \pmod{p}$$
意思是：$a^{p-1}$除以p的余数是1（前提是p是素数且a不是p的倍数）

---

### 8. 集合运算 - 处理一组东西

**集合的基本概念**：
- 集合就是"一堆东西"，用大括号表示
- $\{1, 2, 3\}$ 表示包含1、2、3的集合

**常见运算符**：
- $\in$：属于。$a \in S$ 表示a在集合S中
- $\subset$：子集。$S' \subset S$ 表示S'是S的一部分

**集合减法**：
- $S/a$ 或 $S \setminus \{a\}$：集合S去掉元素a

**在算法中的应用**：
$$f_S = \min_{S' \subset S} f_{S/S'}$$
意思是：在S的所有子集S'中，找$f_{S/S'}$的最小值

**理解技巧**：把集合想象成背包里的物品，集合运算就是添加或移除物品。

---

## 第三部分：函数和方程表示（重点！需要深入理解）

### 9. 函数的完整概念 - 从初中定义到映射

#### 【初中定义】函数 - 一个数对应一个数

**初中课本的定义**：
设在一个变化过程中有两个变量x和y，如果对于x的每一个值，y都有唯一确定的值与之对应，那么y就是x的函数。

**生活例子**：
- 你买东西，每件商品有固定的价格
  - x = 商品编号
  - y = 商品价格
  - 函数：$price(x)$ = 该商品的价格

- 坐公交车，费用取决于距离
  - x = 距离
  - y = 费用
  - 函数：$cost(x)$ = 根据距离计算的费用

**初中理解的关键点**：
1. **一个输入对应一个输出**：输入相同，输出必定相同
2. **唯一性**：不能一个x对应多个y（但可以多个x对应同一个y）

---

#### 【高中扩展】函数 - 映射的观点

**映射的定义**：
设X和Y是两个非空集合，如果存在一个法则f，使得对于X中的每一个元素x，在Y中都有唯一确定的元素y与之对应，那么f就是从X到Y的映射。

**为什么需要映射的观点？**

初中的"函数"把x和y局限为"数"，但映射允许x和y是：
- 数：$f(1) = 2$
- 点：$f((1,2)) = (3,4)$
- 集合：$f(\{1,2\}) = \{3,4,5\}$
- 字符串：$f("hello") = "world"$

**映射的三个关键性质**：
1. **存在性**：X中每个元素都要有对应的Y元素
2. **唯一性**：每个X元素只能对应一个Y元素
3. **方向性**：从X到Y，方向明确

---

#### 【算法竞赛】函数可以有多个参数

**现实需求**：
很多时候，一个结果不是由一个因素决定的，而是由多个因素共同决定。

**例子**：
- 商品价格不仅取决于商品种类，还取决于购买数量
- 成本不仅取决于起点，还取决于终点

**数学表示**：
$$f(x_1, x_2, x_3)$$

**生活例子**：
- 坐出租车费用 = $f(起点, 终点, 时间)$
- 买东西总价 = $f(商品, 数量, 是否会员)$

**在算法中的应用**：
- $w(l, r)$：区间$[l, r]$的某种属性（如权值和）
- $cost(i, j)$：从状态j转移到状态i的代价

**常见思路**

有时我们会把一个参数看作变量，而其他的看作常数（主元法）

---

#### 【深入理解】函数的值可以不止一个数字

**传统观念的局限**：
初中函数$f(x) = y$，y是一个数。

**算法竞赛中的扩展**：
函数的值可以是：
1. **一个数**：$f(x) = 5$（最常见的）
2. **一个向量**：$f(x) = (y_1, y_2, y_3)$（多个数值组成的）
3. **一个集合**：$f(x) = \{a, b, c\}$（返回一组东西）
4. **一个状态**：$f(x) = (重量, 价值)$（多个属性）

**具体例子**：

**例1：点坐标函数**
$$p_i = (p_{i1}, p_{i2}, \ldots, p_{iB})$$
意思是：函数$p(i)$返回第i个点的坐标（一个B维向量）

**例2：策略评价函数**
$$f(x) = (目标函数值, 约束条件值)$$
比如：$f(某个方案) = (总收益, 总成本)$

---

### 10. 动态规划方程 - 递推关系的数学写法

**什么是动态规划方程？**
用数学公式描述"如何从已知状态计算新状态"。

**基本形式**：
$$dp_i = \min_{j < i} \{dp_j + cost(j, i)\}$$

**逐部分解读**：
- $dp_i$：我们要计算的"第i个状态的最优值"
- $=$：通过后面的方式计算得出
- $\min$：在所有选项中取最小值
- $\min$下面的$j < i$：候选范围是所有$j < i$的情况
- 大括号里的内容：每个候选情况的具体计算方式

**等价代码理解**：
```cpp
for (int i = 1; i <= n; i++) {
    dp[i] = INT_MAX;  // 初始化为很大
    for (int j = 0; j < i; j++) {  // j < i 的所有情况
        dp[i] = min(dp[i], dp[j] + cost(j, i));  // 取最小
    }
}
```

**常见变体**：
1. **有限制条件的转移**：
$$dp_i = \min_{j < i \And S_i - S_j > dp_j} \{S_i - S_j\}$$

2. **多维状态**：
$$f_{i,m} = \min_{a \in S} f_{S/a, m - w_a}$$

3. **区间转移**：
$$dp_i = \min_{L(i) \le j < R(i)} \{dp_j + cost(j, i)\}$$

---

### 11. 前缀和表示 - 快速求区间和的工具

**前缀和的定义**：
$pre_i = \sum_{k=1}^{i} a_k$，即前i个数的总和。

**为什么要用前缀和？**
可以快速计算任意区间的和。

**公式**：
区间$[l, r]$的和 = $pre_r - pre_{l-1}$

**例子**：
- $pre_0 = 0$（前0个数的和，自然是0）
- $pre_1 = a_1$
- $pre_2 = a_1 + a_2$
- $pre_n = a_1 + a_2 + \ldots + a_n$

**应用场景**：
快速计算区间$[3, 7]$的和：
$$sum = pre_7 - pre_2 = (a_1 + \ldots + a_7) - (a_1 + a_2) = a_3 + \ldots + a_7$$

---

## 第四部分：集合和区间（高一数学基础）

### 12. 集合表示 - 数学中的"容器"

**集合的基本符号**：
- $\{a, b, c\}$：包含a、b、c三个元素的集合
- $\{x | 条件\}$：满足某条件的所有x组成的集合

**常见集合写法**：
- $[L, R)$：从L到R但不包括R的区间（左闭右开）
- $[L, R]$：从L到R且包括R和L的区间（闭区间）
- $\{1, 2, \ldots, n\}$：从1到n的所有整数

**集合在算法中的应用**：

**例：状态集合**
$$f_S = \min_{S' \subset S} f_{S/S'}$$
- $S$：当前已考虑的物品集合
- $S'$：某个子集
- $S/S'$：集合S去掉子集S'后的剩余部分

---

### 13. 集合运算符 - 操作集合的工具

**$\in$ 符号（属于）**：
$a \in S$ 表示元素a在集合S中。

**例子**：
- $3 \in \{1, 2, 3\}$ 是对的
- $4 \in \{1, 2, 3\}$ 是错的

**$\subset$ 符号（子集）**：
$S' \subset S$ 表示集合S'是集合S的一部分。

**例子**：
- $\{1, 2\} \subset \{1, 2, 3\}$ 是对的
- $\{1, 4\} \subset \{1, 2, 3\}$ 是错的

---

## 第五部分：复杂度和算法表示（算法竞赛专属）

### 14. 时间复杂度 $O(\cdot)$ - 算法效率的衡量

**什么是时间复杂度？**
表示算法运行时间随数据规模增长的趋势。

**常见复杂度**：
- $O(1)$：常数时间，无论数据多大都很快
- $O(n)$：线性时间，数据量翻倍，时间也翻倍
- $O(n \log n)$：比$O(n^2)$快，比$O(n)$慢一点
- $O(n^2)$：平方时间，数据量翻倍，时间变成4倍

**生活类比**：
- $O(1)$：直接看答案（一眼看出）
- $O(n)$：逐个检查（一个一个看）
- $O(n^2)$：每两个都比较一下（两两配对）

**判断方法**：
看代码中的循环：
```cpp
for (int i = 0; i < n; i++) {      // O(n)
    for (int j = 0; j < n; j++) {  // O(n)
        // O(n) * O(n) = O(n^2)
    }
}
```

---

### 15. 维度表示 - 多维空间的概念

**一维**：线上的点，只需一个坐标
- $n$ 表示长度
- 点坐标：$x$

**二维**：平面上的点，需要两个坐标
- $n \times m$ 表示$n$行$m$列的网格
- 点坐标：$(x, y)$

**三维**：空间中的点，需要三个坐标
- 点坐标：$(x, y, z)$

**B维**：抽象的高维空间
- 点坐标：$(p_1, p_2, \ldots, p_B)$
- 例如：$p_i = (p_{i1}, p_{i2}, \ldots, p_{iB})$ 表示第i个点的B维坐标

**理解技巧**：
- 一维像街道上的门牌号
- 二维像教室里的座位（第几排第几列）
- 三维像楼房里的房间（楼层、排、列）
- B维抽象但数学处理方式相同

---

## 第六部分：几何和向量（高二数学基础）

### 16. 斜率公式 - 直线的倾斜程度

**直线方程**：
$$y = kx + b$$

**各部分含义**：
- $k$：斜率，表示直线的倾斜程度
  - $k > 0$：向右上倾斜
  - $k < 0$：向右下倾斜
  - $k = 0$：水平线
- $b$：截距，直线与y轴的交点位置

**斜率计算**：
两点$(x_1, y_1)$和$(x_2, y_2)$连线的斜率：
$$k = \frac{y_2 - y_1}{x_2 - x_1}$$

**在斜率优化中的应用**：
决策点$(x_j, y_j)$形成的"凸包"，通过斜率比较找出最优决策点。

---

### 17. 向量表示 - 多维坐标

**向量是什么？**
一组有序的数，表示多维空间中的位置或方向。

**表示方法**：
- $\vec{v} = (v_1, v_2, v_3)$：三维向量
- $\mathbf{p} = (p_1, p_2, \ldots, p_B)$：B维向量

**在算法中的应用**：
- 点坐标：$p_i = (p_{i1}, p_{i2}, \ldots, p_{iB})$
- 查询矩形：由两个向量$L_j$和$R_j$定义

**理解技巧**：
向量就是"多个数字打包在一起"，像一个袋子装了多个数值。

---

## 总结：如何阅读数学公式

**阅读顺序**：
1. 先看符号的基本含义（查本文）
2. 理解符号的限制条件（下标、范围）
3. 理解符号的运算内容（括号里的内容）
4. 转换为代码思维（思考如何用程序实现）

**遇到不懂的公式怎么办**：
1. 先拆分符号，逐个理解
2. 用生活例子类比
3. 写出等价的代码理解
4. 多看几次，逐步熟悉
5. 上网搜索，问问AI助手（这个文章就是AI写的，我只稍作修改）
6. 问老师或同学：如果你问我，就算我不会也会先自己搞懂，再回答你；其他同学也要有这个精神。

**学习建议**：
- 不要死记硬背，要理解含义
- 在实践中遇到公式时回来查阅
- 自己尝试把公式翻译成代码

**下一步学习**：
掌握了这些基础符号后，可以开始看博客里的其他文章了，遇到不懂的符号就回来查阅这个入门指南。祝你学习愉快！

